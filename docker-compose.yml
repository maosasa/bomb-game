services:
  backend:
    build: ./server
    ports:
      - "8000:8000"
  frontend:
    build:
      context: ./client
      args:
        VITE_WS_URL: ws://localhost:8000/ws
    depends_on:
      - backend
    ports:
      - "5173:80"